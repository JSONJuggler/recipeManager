import React, { Fragment } from "react";

const pstyle = {
  fontColor: "black",
  fontSize: "20px",
  marginTop: "50px"
};

const Landing = () => {
  return (
    <Fragment>
      <p style={pstyle}>Register</p>
    </Fragment>
  );
};

export default Landing;
